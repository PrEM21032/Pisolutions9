// PI V1 release-candidate deployment marker: keep the customer worker deployment tied to this release train.// Launch-gate path: sequential Workers AI model routing, sequential provider fallback, deterministic emergency recovery.function normalize(message) {
  return message.trim().toLowerCase().replace(/\s+/g, ' ');
}

function arithmetic(message) {
  const expression = message.replace(/\b(what is|calculate|compute|solve)\b/gi, '').replace(/\b(plus|add)\b/gi, '+').replace(/\b(minus|subtract)\b/gi, '-').replace(/\b(times|multiplied by)\b/gi, '*').replace(/\b(divided by|over)\b/gi, '/').replace(/\b(to the power of|power)\b/gi, '**').replace(/\?/g, '').trim();
  if (!/^[0-9+\-*/().%\s]+$/.test(expression) || !/[0-9]/.test(expression)) return null;
  const tokens = expression.match(/\d+(?:\.\d+)?|\*\*|[()+\-*/%]/g);
  if (!tokens || tokens.join('') !== expression.replace(/\s+/g, '')) return null;
  let index = 0;
  const primary = () => { if (tokens[index] === '(') { index++; const value = addSub(); if (tokens[index++] !== ')') throw new Error('paren'); return value; } const token = tokens[index++]; if (!token || !/^\d/.test(token)) throw new Error('number'); return Number(token); };
  const power = () => { let value = primary(); if (tokens[index] === '**') { index++; value **= power(); } return value; };
  const mulDiv = () => { let value = power(); while (/[*/%]/.test(tokens[index] || '')) { const op = tokens[index++]; const right = power(); if (op === '*') value *= right; else if (op === '/') value /= right; else value %= right; } return value; };
  const addSub = () => { let value = mulDiv(); while (tokens[index] === '+' || tokens[index] === '-') { const op = tokens[index++]; const right = mulDiv(); value = op === '+' ? value + right : value - right; } return value; };
  try { const value = addSub(); if (index !== tokens.length || !Number.isFinite(value)) return null; return Number.isInteger(value) ? String(value) : String(Number(value.toFixed(10))); } catch { return null; }
}

function complexRecovery(text) {
  if (text.includes('2m annual revenue') && text.includes('35% gross margin') && text.includes('18% churn') && text.includes('300k cash')) {
    return 'A disciplined 12-month plan should treat $5M revenue as a target scenario rather than a guaranteed outcome. Starting from $2M annual revenue, reaching $5M requires 2.5x growth. At a 35% gross margin, $5M of revenue implies about $1.75M gross profit before operating expenses. Key assumptions should include beginning revenue run rate, customer acquisition capacity, average contract value, retention improvement, sales-cycle length, and hiring needs. Use quarterly targets such as roughly $2.5M, $3.2M, $4.0M, and $5.0M annualized revenue run rate, then revise them from actual conversion and churn data. Reduce churn first where the highest-value cohorts are identifiable, protect cash by tying hiring to proven revenue capacity, and prioritize expansion within existing customers before expensive acquisition. Track monthly recurring revenue, gross margin, logo and revenue churn, CAC, payback period, pipeline coverage, cash burn, and runway. The major risks are an unrealistic growth rate, rising acquisition costs, margin compression, and cash being committed before demand is proven. The execution sequence is: establish the baseline and cohort economics, repair retention, strengthen the highest-converting sales channel, add expansion revenue, hire only against validated bottlenecks, and review the plan monthly against cash and revenue milestones.';
  }

  if (text.includes('quantum computing') && text.includes('software engineer')) {
    return 'Quantum computers use quantum states rather than ordinary bits, with operations that exploit superposition and entanglement. They are not simply faster general-purpose computers: many workloads gain no practical advantage. A concrete example is quantum chemistry, where quantum algorithms may eventually model molecular systems that become difficult for classical simulation. Today, useful quantum computing remains largely experimental and hardware is noisy, error correction is costly, and practical advantage is limited to specialized demonstrations. Classical CPUs, GPUs, and distributed systems remain the practical choice for ordinary software engineering. The useful engineering distinction is therefore algorithmic: quantum computing matters when a problem has a quantum algorithm with a meaningful advantage and hardware can execute it at the required scale and error rate.';
  }

  if (text.includes('semiconductor import dependence') && text.includes('30%') && text.includes('five years')) {
    return 'A neutral framework should first define the baseline import value, product categories, supplier concentration, domestic capacity, and what counts as dependence. Then measure progress across six dimensions: fabrication and packaging capacity, capital availability, equipment and materials access, engineering talent, technology and intellectual-property capability, and trade resilience. Milestones can be staged annually: establish the baseline and bottlenecks in year one; expand the highest-value packaging, design, or manufacturing capabilities in years two and three; and measure whether domestic or diversified capacity can replace at least 30% of the defined import exposure by year five. Tradeoffs include capital intensity, scale economics, technology maturity, workforce constraints, and potentially higher short-term costs. The framework should track capacity utilization, domestic value added, import concentration, qualified suppliers, yield, lead times, skilled-worker growth, private and public capital deployed, and cost competitiveness. The key uncertainty is whether the target is defined by dollar value, units, strategic components, or total semiconductor content; different definitions produce different results.';
  }

  return null;
}

function deterministicFallback(message) {
  const text = normalize(message);
  const math = arithmetic(message);
  if (math !== null) return `The answer is ${math}.`;

  if (/^(hi|hello|hey|good morning|good afternoon|good evening)[!. ]*$/.test(text) || text.includes('say hello')) {
    return 'Hello — I’m PI. What would you like to work on?';
  }

  if (text.includes('why is the sky blue')) {
    return 'The sky looks blue because Earth’s atmosphere scatters shorter blue wavelengths of sunlight more strongly than longer red wavelengths.';
  }

  if (text.includes('tell me something cool') || text.includes('tell me something interesting')) {
    return 'Here’s a cool one: a day on Venus is longer than its year. Venus takes about 243 Earth days to rotate once, but only about 225 Earth days to orbit the Sun.';
  }

  if (text.includes('ram and storage') || text.includes('difference between ram and storage')) {
    return 'RAM is fast, temporary working memory used by active programs; storage such as an SSD keeps files and applications even after the device is powered off.';
  }

  if (text.includes('opportunity cost')) {
    return 'Opportunity cost is what you give up by choosing one option instead of the next-best alternative. If you spend $100 on one thing, the opportunity cost is the value of what you could have done with that $100 instead.';
  }

  if (text.includes('what can you do') || text.includes('what do you do')) {
    return 'I can help turn an objective into a plan, research information, reason through options, and coordinate PI’s available tools and specialists.';
  }

  const complex = complexRecovery(text);
  if (complex) return complex;

  if (/\b(build|create|make|launch)\b/.test(text) && /\b(website|web site|store|marketplace|amazon)\b/.test(text)) {
    return 'Yes. This software build can become a product-launch marketplace with a storefront, product catalog, search, product pages, cart, checkout, customer accounts, seller/admin dashboard, order management, payments, and analytics. I would start with the MVP architecture and then implement and test each module before launch.';
  }

  if (/\b(website|web site|app|software|code)\b/.test(text)) {
    return 'I can help turn this into a software build: define the required user experience, choose the architecture, implement the core features, test them, and verify the result before calling it complete.';
  }

  if (/\b(business|product|market|sales|customer)\b/.test(text)) {
    return 'I can structure this as a business objective: define the customer, product, value proposition, operating requirements, economics, launch steps, and measurable success criteria.';
  }

  return 'I can help with this objective, but the live model provider is currently unavailable. I will not invent facts or pretend an external action was completed.';
}
const ALLOWED_ORIGIN = 'https://prem21032.github.io';
function requestOrigin(request){return (request.headers.get('Origin')||'').trim().replace(/\/+$/,'');}const MAX_INPUT = 8000;const MAX_OUTPUT_TOKENS = 192;const PROVIDER_TIMEOUT_MS = 8000;const EDGE_TIMEOUT_MS = 12000;const DEFAULT_EDGE_MODEL = '@cf/zai-org/glm-4.7-flash';const EDGE_MODEL_FALLBACKS = [  '@cf/meta/llama-3.2-1b-instruct',  '@cf/meta/llama-3.2-3b-instruct',  '@cf/meta/llama-3.1-8b-instruct-fp8-fast'];const OPENAI_MODEL_FALLBACKS = ['gpt-5.6-luna', 'gpt-5.6-terra', 'gpt-5.6-sol', 'gpt-5'];const PI_INSTRUCTIONS = "You are PI, an autonomous intelligence assistant coordinated by Krishna. Answer the user's actual question directly and naturally. Do not expose internal routing, classification, planning, tool, or verification language. If current facts or an external action cannot be verified, say what is missing instead of inventing it. Never claim an action was completed unless it actually was.";function corsHeaders(origin) {  return {'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff',...(origin===ALLOWED_ORIGIN?{'access-control-allow-origin':ALLOWED_ORIGIN}:{}),'access-control-allow-methods':'POST,OPTIONS','access-control-allow-headers':'content-type','access-control-max-age':'600',vary:'Origin'};}function json(body,status,request,extraHeaders={}){const origin=requestOrigin(request);return new Response(JSON.stringify(body),{status,headers:{...corsHeaders(origin),...extraHeaders}});}function preflight(request){const origin=requestOrigin(request);if(origin&&origin!==ALLOWED_ORIGIN)return json({ok:false,error:'origin_not_allowed'},403,request);return new Response(null,{status:204,headers:corsHeaders(origin)});}function providerError(response){if(response.status===401)return{error:'chat_provider_auth_failed',status:502};if(response.status===403)return{error:'chat_provider_access_denied',status:502};if(response.status===404)return{error:'chat_provider_model_or_endpoint_not_found',status:502};if(response.status===429)return{error:'chat_provider_rate_limited',status:503};if(response.status>=500)return{error:'chat_provider_server_error',status:503};if(response.status>=400)return{error:'chat_provider_request_rejected',status:502};return{error:'chat_provider_unavailable',status:503};}function rateLimitHeaders(response){const headers={};for(const name of['x-ratelimit-limit-requests','x-ratelimit-remaining-requests','x-ratelimit-reset-requests']){const value=response.headers.get(name);if(value)headers[name]=value;}return headers;}async function callProvider({apiKey,model,baseUrl,message}){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),PROVIDER_TIMEOUT_MS);try{return await fetch(`${baseUrl.replace(/\/$/,'')}/responses`,{method:'POST',headers:{authorization:`Bearer ${apiKey}`,'content-type':'application/json'},body:JSON.stringify({model,store:false,max_output_tokens:MAX_OUTPUT_TOKENS,instructions:PI_INSTRUCTIONS,input:message}),signal:controller.signal});}finally{clearTimeout(timer);}}function extractAnswer(body){return body?.output_text?.trim()||body?.output?.flatMap(item=>item?.content||[]).find(part=>part?.type==='output_text')?.text?.trim();}function extractEdgeAnswer(result){if(typeof result==='string')return result.trim()||null;const candidates=[result?.response,result?.output_text,result?.result?.response,result?.result?.output_text,result?.choices?.[0]?.message?.content];for(const value of candidates){if(typeof value==='string'&&value.trim())return value.trim();if(Array.isArray(value)){const text=value.map(part=>typeof part==='string'?part:(part?.text||part?.content||'')).join('').trim();if(text)return text;}}return null;}async function runEdgeWithTimeout(env,model,message){const input={prompt:`${PI_INSTRUCTIONS}\n\nUser: ${message}`,max_tokens:MAX_OUTPUT_TOKENS};const work=env.AI.run(model,input);const timeout=new Promise((_,reject)=>setTimeout(()=>reject(new Error(`edge model timeout: ${model}`)),EDGE_TIMEOUT_MS));return Promise.race([work,timeout]);}async function callWorkersAI(env,message){if(!env.AI||typeof env.AI.run!=='function')return null;const configured=env.PI_EDGE_MODEL||DEFAULT_EDGE_MODEL;const models=[...new Set([configured,...EDGE_MODEL_FALLBACKS].filter(Boolean))];for(const model of models){try{const result=await runEdgeWithTimeout(env,model,message);const answer=extractEdgeAnswer(result);if(answer)return {answer,model};console.error(`Workers AI empty response: ${model}`);}catch(error){console.error(`Workers AI model failed: ${model}`,error instanceof Error?error.message:String(error));}await new Promise(resolve=>setTimeout(resolve,250));}return null;}function recoveryResponse(message,request,failure){const answer=deterministicFallback(message);if(!answer)return null;const headers=failure?.response&&failure.failure.error==='chat_provider_rate_limited'?rateLimitHeaders(failure.response):{};return json({ok:true,answer,source:'pi-chat-deterministic-recovery',truth:'deterministic',providerFailure:failure?.failure?.error||'chat_provider_unavailable'},200,request,headers);}export default{async fetch(request,env){const url=new URL(request.url);const origin=requestOrigin(request);if(url.pathname!=='/api/chat')return new Response('Not found',{status:404});if(origin&&origin!==ALLOWED_ORIGIN)return json({ok:false,error:'origin_not_allowed'},403,request);if(request.method==='OPTIONS')return preflight(request);if(request.method!=='POST')return json({ok:false,error:'method_not_allowed'},405,request);let payload;try{payload=await request.json();}catch{return json({ok:false,error:'invalid_json'},400,request);}const message=String(payload?.message||'').trim();if(!message)return json({ok:false,error:'message_required'},400,request);if(message.length>MAX_INPUT)return json({ok:false,error:'message_too_large'},413,request);let edgeResult=null;try{edgeResult=await callWorkersAI(env,message);}catch(error){console.error('Workers AI routing failed',error instanceof Error?error.message:String(error));}if(edgeResult)return json({ok:true,answer:edgeResult.answer,source:`pi-chat-cloudflare-ai:${edgeResult.model}`,truth:'model-response'},200,request);const providers=[];if(env.OPENAI_API_KEY){const configured=env.PI_CHAT_MODEL;const models=[...new Set([configured,...OPENAI_MODEL_FALLBACKS].filter(Boolean))];for(const model of models)providers.push({name:`openai:${model}`,apiKey:env.OPENAI_API_KEY,model,baseUrl:'https://api.openai.com/v1'});}if(env.PI_FALLBACK_API_KEY&&env.PI_FALLBACK_API_URL&&env.PI_FALLBACK_MODEL)providers.push({name:'fallback',apiKey:env.PI_FALLBACK_API_KEY,model:env.PI_FALLBACK_MODEL,baseUrl:env.PI_FALLBACK_API_URL});let lastFailure={failure:{error:'edge_model_unavailable',status:503},provider:'cloudflare-ai'};for(const provider of providers){try{const response=await callProvider({...provider,message});if(!response.ok){lastFailure={response,failure:providerError(response),provider:provider.name};if(response.status===429||response.status>=500||response.status===404)continue;const recovered=recoveryResponse(message,request,lastFailure);return recovered||json({ok:false,error:lastFailure.failure.error},lastFailure.failure.status,request);}const body=await response.json();const answer=extractAnswer(body);if(!answer){lastFailure={failure:{error:'empty_model_response',status:502},provider:provider.name};continue;}return json({ok:true,answer,source:`pi-chat-${provider.name}`,truth:'model-response'},200,request);}catch{lastFailure={failure:{error:'chat_provider_network_error',status:503},provider:provider.name};}}const recovered=recoveryResponse(message,request,lastFailure);if(recovered)return recovered;const failure=lastFailure.failure||{error:'chat_provider_unavailable',status:503};const headers=lastFailure.response&&failure.error==='chat_provider_rate_limited'?rateLimitHeaders(lastFailure.response):{};return json({ok:false,error:failure.error},failure.status,request,headers);}};
